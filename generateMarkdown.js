// function to generate markdown for README
const generateMarkdown = (thisIsMe) => {
  if (!thisIsMe) {
    return "";
  }
};

// const readME = (readMeArr) => {
//   return `
//   # ${readMeArr
//   .filter(({ }))}
//   `
// }

module.exports = generateMarkdown;
